<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>HollowRun.Worker</AssemblyName>
    <RootNamespace>HollowRun.Worker</RootNamespace>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Platforms>AnyCPU;x64</Platforms>
    <DebugType>none</DebugType>
    <DebugSymbols>false</DebugSymbols>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Facepunch.Steamworks" Version="2.3.3" />
  </ItemGroup>

  <Target Name="CopySteamApiDll" AfterTargets="Build;Publish">
    <Copy SourceFiles="$(NuGetPackageRoot)facepunch.steamworks\2.3.3\content\steam_api64.dll" DestinationFolder="$(TargetDir)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="$(NuGetPackageRoot)facepunch.steamworks\2.3.3\content\steam_api64.dll" DestinationFolder="$(PublishDir)" SkipUnchangedFiles="true" Condition="'$(PublishDir)' != ''" />
  </Target>

  <Target Name="RemoveLegacyWorkerArtifacts" AfterTargets="Publish">
    <ItemGroup>
      <LegacyWorkerArtifact Include="$(PublishDir)HollowRun.Worker.exe" />
      <LegacyWorkerArtifact Include="$(PublishDir)HollowRun.Worker.dll" />
      <LegacyWorkerArtifact Include="$(PublishDir)HollowRun.Worker.deps.json" />
      <LegacyWorkerArtifact Include="$(PublishDir)HollowRun.Worker.runtimeconfig.json" />
    </ItemGroup>
    <Delete Files="@(LegacyWorkerArtifact)" />
  </Target>

</Project>
