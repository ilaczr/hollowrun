{
  "name": "hollowrun",
  "version": "1.0.0",
  "description": "A zero-credential, local Steam idling tool for Windows that connects directly to your active Steam desktop client instance using Steamworks IPC.",
  "main": "electron-main.cjs",
  "scripts": {
    "setup": "npm ci --prefix backend && npm ci --prefix frontend",
    "start": "electron .",
    "dev": "concurrently -k \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build:frontend": "npm run build --prefix frontend",
    "build:worker": "dotnet publish HollowRun.Worker/HollowRun.Worker.csproj -c Release -r win-x64 --self-contained false -o HollowRun.Worker/publish",
    "build": "npm run build:frontend && electron-builder --win",
    "test": "npm test --prefix backend && node --test window-state.test.cjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "build": {
    "appId": "com.hollowrun.app",
    "productName": "HollowRun",
    "asar": false,
    "win": {
      "target": ["nsis", "portable"],
      "icon": "frontend/public/hollowrun.svg"
    },
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "electron-main.cjs",
      "window-state.cjs",
      "backend/**/*",
      "frontend/dist/**/*",
      "frontend/public/**/*",
      "HollowRun.Worker/publish/**/*"
    ]
  },
  "keywords": [],
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.4",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3"
  }
}
